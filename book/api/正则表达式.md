```
\d
表示匹配数字
\w
表示匹配字母或数字
.
表示匹配任意字符
*
用于后缀，表示任意个单位
+
同上，但是最少有一个单位
?
表示该单位有1或0个
\s
可以匹配一个空格
[]
表示范围，用于精确匹配
|
表示或，需要使用（）括住单位
^ $
用于表示正则的开始和结束
/g
用于表示全局匹配，在应用场景下，最好是加上

注：
若有特殊符号隔开要匹配的单位时，需要在特殊符号两侧加上\，就是转义
```
